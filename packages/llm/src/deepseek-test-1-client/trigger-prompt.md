<!--
  deepseek-test-1 触发 prompt

  说明：此文件为 deepseek-test-1 client 的「第一个 user 消息」前缀触发文本。
  当前内容为空——触发逻辑已迁移至系统消息（code.md）中的 <think-guidance> 部分。

  约定：此 md 的全部正文（去除本注释后）会作为触发文本，拼在所有 skill 之前，
  组成 client 注入的第一个 user 消息。
-->
