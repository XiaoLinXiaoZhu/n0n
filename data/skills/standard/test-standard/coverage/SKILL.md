---
description: 覆盖策略——优先覆盖核心逻辑、边界条件、回归点、安全路径。
activation: init
order: 248
---

# 覆盖策略

不追求 100% 覆盖率。优先覆盖：

1. 核心业务逻辑（计算、状态转换、校验规则）
2. 边界条件（空输入、极限值、边界值）
3. 已知回归点（曾经出过 bug 的地方）
4. 安全敏感路径（权限、认证、数据完整性）

对于 CRUD/样板代码，除非有非平凡逻辑，否则不必为测而测。
