---
description: 测试属性——原子化、独立隔离、只测公共接口、谨慎使用 Mock。
activation: init
order: 243
---

- **小而原子化**：每个测试只验证一个行为，失败时一眼定位问题
- **彼此独立隔离**：测试之间不共享可变状态，执行顺序不影响结果
- **只测公共接口**：测试通过公开 API 验证行为，不测私有实现细节
- **谨慎使用 Mock**：优先使用真实对象（或轻量 fake），仅对不可控的外部依赖（网络、时钟、文件系统）使用 mock
