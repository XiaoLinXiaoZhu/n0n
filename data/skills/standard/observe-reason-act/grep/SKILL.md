---
description: 优先用 rg（ripgrep）而非 grep——更快、默认递归、自动尊重 .gitignore。
activation: init
order: 435
---

# 优先用 rg 而非 grep

- 优先用 `rg`（ripgrep）而非 `grep`——更快、默认递归、自动尊重 `.gitignore`。
- 注意 `rg` 的 or `|` 不需要转义，使用 `rg "A|B"` 而不是 `rg "A\|B"`
