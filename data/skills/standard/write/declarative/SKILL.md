---
description: write 核心原则——声明式覆盖，关心目标状态而非变更路径。
activation: init
order: 121
---

修改文件时，你关心的是**目标状态**（"文件应该长什么样"），而非**变更路径**（"文件应该怎么改"）。`write` 是声明式的——你直接输出目标状态，文件系统覆盖即完成。这比描述"在第 N 行插入/删除/替换"更安全、更确定。
