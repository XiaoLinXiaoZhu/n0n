# TODO — 缓存生命周期管理

> 成本分析已归入 wiki 词条：【心跳策略】、【压缩策略】
> 可视化图表仍在 `docs/analysis/` 目录下

---

## Compression — 上下文压缩（未实现）

详见 wiki：`工作日志/压缩开发计划`

### 实现清单

- [ ] 通用压缩接口（输入 `DomainMessage[]`，输出摘要 `DomainMessage`）
- [ ] 压缩后端选择逻辑
- [ ] 触发策略（固定间隔 / 上下文大小阈值 / 成本预估）
- [ ] 压缩质量保障
- [ ] 与 fairy `view.ts` 跳变窗口机制协调
